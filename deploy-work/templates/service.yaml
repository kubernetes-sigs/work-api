apiVersion: v1
kind: Service
metadata:
  name: {{ include "deploy-work.fullname" . }}
  labels:
    {{- include "deploy-work.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "deploy-work.selectorLabels" . | nindent 4 }}
